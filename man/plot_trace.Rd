% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trace.R
\name{plot_trace}
\alias{plot_trace}
\title{Plot the Markov cohort trace}
\usage{
plot_trace(results, decisions = NULL, states = NULL)
}
\arguments{
\item{results}{Either a verbose result from \code{\link[=run_twig]{run_twig()}} (a list containing a
\code{markov_trace} element) or a \code{markov_trace} array directly. The array has
dimensions cycle, state, and decision.}

\item{decisions}{An optional character vector selecting which decisions to
show. Defaults to all decisions.}

\item{states}{An optional character vector selecting which states to show.
Defaults to all states.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the proportion of the cohort occupying each Markov state over time, one
panel per decision. The trace is produced by running a Markov twig with
\code{verbose = TRUE} (see \code{\link[=run_twig]{run_twig()}}), which returns the \code{markov_trace} array.
}
\examples{
# A small cohort trace array (cycle x state x decision)
trace <- array(
  c(1, 0.9, 0.8, 0, 0.1, 0.2,     # decision A: Alive, then Dead
    1, 0.8, 0.6, 0, 0.2, 0.4),    # decision B
  dim = c(3, 2, 2),
  dimnames = list(cycle = c(0, 1, 2),
                  state = c("Alive", "Dead"),
                  decision = c("A", "B")))
plot_trace(trace)
}
\seealso{
\code{\link[=run_twig]{run_twig()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ce.R
\name{plot_ce}
\alias{plot_ce}
\title{Plot the cost-effectiveness plane}
\usage{
plot_ce(
  sim_ev,
  mean_ev = NULL,
  col_names = c("cost", "utility"),
  frontier = TRUE,
  show_points = TRUE
)
}
\arguments{
\item{sim_ev}{A 3D array of simulated expected values with dimensions
decision, payoff (cost and effect), and simulation, as returned in
\code{run_twig()$sim_ev}.}

\item{mean_ev}{An optional decision-by-payoff matrix of mean expected values
(\code{run_twig()$mean_ev}). If \code{NULL}, the means are computed from \code{sim_ev}.}

\item{col_names}{A length-2 character vector naming the cost and effect
payoffs. Default \code{c("cost", "utility")}.}

\item{frontier}{Logical; if \code{TRUE} (default) the efficiency frontier through
the non-dominated strategies (from \code{\link[=calculate_icers]{calculate_icers()}}) is drawn.}

\item{show_points}{Logical; if \code{TRUE} (default) the per-simulation PSA cloud
is shown. Set to \code{FALSE} to plot only the strategy means.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the cost-effectiveness (CE) plane from the simulated expected values
returned by \code{\link[=run_twig]{run_twig()}}. Each probabilistic-sensitivity-analysis (PSA) draw
is shown as a point (the uncertainty cloud), the mean of each strategy is
highlighted, and the efficiency frontier connecting the non-dominated
strategies can be overlaid.
}
\examples{
set.seed(1)
sim_ev <- array(
  c(rnorm(50, 1000, 50), rnorm(50, 2000, 80),   # costs (A, B)
    rnorm(50, 0.8, 0.02), rnorm(50, 0.9, 0.02)), # effects (A, B)
  dim = c(2, 2, 50),
  dimnames = list(c("A", "B"), c("cost", "utility"), NULL))
plot_ce(sim_ev)
}
\seealso{
\code{\link[=plot_ceac]{plot_ceac()}}, \code{\link[=calculate_icers]{calculate_icers()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_decision_tree.R
\name{draw_decision_tree}
\alias{draw_decision_tree}
\title{Draw the decision tree of a twig}
\usage{
draw_decision_tree(twig_obj, output = c("graph", "dot"), prob_labels = TRUE)
}
\arguments{
\item{twig_obj}{A twig object created with \code{\link[=twig]{twig()}}.}

\item{output}{Either \code{"graph"} (default) to return a rendered \pkg{DiagrammeR}
htmlwidget, or \code{"dot"} to return the Graphviz DOT string.}

\item{prob_labels}{Logical; if \code{TRUE} (default) edges are labeled with the
option name and its probability-function name.}
}
\value{
A \pkg{DiagrammeR} htmlwidget when \code{output = "graph"}, otherwise a
length-1 character string of Graphviz DOT (invisibly).
}
\description{
Produces a visualization of the event (chance-node) structure of a twig as a
decision tree: a square decision node branches into each strategy, and each
strategy expands the shared sequence of chance nodes (events) down to the
terminal outcomes.
}
\details{
The Graphviz DOT description is generated in pure R (no extra dependency). To
\emph{render} the diagram (\code{output = "graph"}), the suggested package
\pkg{DiagrammeR} must be installed; otherwise request \code{output = "dot"} to get
the DOT string, which can be rendered by any Graphviz engine.
}
\examples{
mytwig <- twig() +
  decisions(names = c(Treat, DoNotTreat)) +
  event(name = progress,
        options = c(yes, none),
        probs = c(pProgress, leftover),
        transitions = c(sick, healthy)) +
  payoffs(names = c(cost))

# Get the Graphviz DOT (no extra package needed):
cat(draw_decision_tree(mytwig, output = "dot"))

# Render it (requires the suggested DiagrammeR package):
if (requireNamespace("DiagrammeR", quietly = TRUE)) {
  draw_decision_tree(mytwig)
}
}
\seealso{
\code{\link[=draw_transitions]{draw_transitions()}}, \code{\link[=run_twig]{run_twig()}}
}

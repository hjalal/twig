% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_transitions.R
\name{draw_transitions}
\alias{draw_transitions}
\title{Draw the Markov state-transition diagram}
\usage{
draw_transitions(
  x,
  params = NULL,
  n_cycles = 1,
  decision = NULL,
  cycle = 1,
  output = c("graph", "dot"),
  digits = 3,
  threshold = 0
)
}
\arguments{
\item{x}{One of: a verbose \code{\link[=run_twig]{run_twig()}} result (a list with a
\code{markov_trans_probs} element), a \code{markov_trans_probs} array, or a twig
object. When a twig object is supplied, \code{params} is required and the model
is run (single simulation) to obtain the probabilities.}

\item{params}{A named list or data frame of parameters, required only when
\code{x} is a twig object.}

\item{n_cycles}{Number of cycles to use when \code{x} is a twig object that must be
run. Default 1.}

\item{decision}{The decision to display, as a name or index. Defaults to the
first decision.}

\item{cycle}{The cycle to display when the transition probabilities are
cycle-dependent. Default 1.}

\item{output}{Either \code{"graph"} (default, a \pkg{DiagrammeR} htmlwidget) or
\code{"dot"} (the Graphviz DOT string).}

\item{digits}{Number of digits for the probability edge labels. Default 3.}

\item{threshold}{Transitions with probability at or below this value are
omitted. Default 0 (only nonzero transitions are drawn).}
}
\value{
A \pkg{DiagrammeR} htmlwidget when \code{output = "graph"}, otherwise a
Graphviz DOT string.
}
\description{
Visualizes the state-transition structure of a Markov twig as a directed
graph, with edges labeled by their transition probabilities. Because the
probabilities depend on the decision (and possibly the cycle), the diagram is
drawn for a single decision and cycle.
}
\details{
The transition probabilities are taken from a verbose \code{\link[=run_twig]{run_twig()}} result, or
computed by running the model when a twig object and parameters are supplied.
The Graphviz DOT description is generated in pure R; rendering
(\code{output = "graph"}) requires the suggested package \pkg{DiagrammeR}.
}
\examples{
# A 2-state transition array (state x dest x decision)
tp <- array(
  c(0.9, 0, 0.1, 1,      # decision A
    0.8, 0, 0.2, 1),     # decision B
  dim = c(2, 2, 2),
  dimnames = list(state = c("Alive", "Dead"),
                  dest = c("Alive", "Dead"),
                  decision = c("A", "B")))
cat(draw_transitions(tp, output = "dot"))

if (requireNamespace("DiagrammeR", quietly = TRUE)) {
  draw_transitions(tp, decision = "A")
}
}
\seealso{
\code{\link[=draw_decision_tree]{draw_decision_tree()}}, \code{\link[=run_twig]{run_twig()}}
}

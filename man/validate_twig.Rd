% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_twig.R
\name{validate_twig}
\alias{validate_twig}
\title{Validate a twig model and its functions before running}
\usage{
validate_twig(twig_obj, params = NULL, envir = parent.frame())
}
\arguments{
\item{twig_obj}{A twig object created with \code{\link[=twig]{twig()}}.}

\item{params}{An optional named list or data frame of parameters, as passed
to \code{\link[=run_twig]{run_twig()}}. When supplied, function arguments are checked against the
parameter names; when \code{NULL}, only function existence and core arguments are
checked.}

\item{envir}{The environment in which the user-defined functions are searched
for. Defaults to the calling environment.}
}
\value{
Invisibly \code{TRUE} when validation passes. Otherwise an error is raised
listing every problem found.
}
\description{
A pre-flight check that complements the internal twig-structure validation by
verifying that the probability, payoff, and initial-probability
\emph{functions} referenced by a twig are defined and that their arguments are
consistent with the model's core arguments and the supplied parameters.
}
\details{
Unlike the checks performed inside \code{\link[=run_twig]{run_twig()}}, \code{validate_twig()} collects
\emph{all} problems it finds and reports them together, which makes it easier to
fix several issues at once before launching a (potentially long) simulation.
}
\examples{
mytwig <- twig() +
  decisions(names = c(A, B)) +
  states(names = c(Alive, Dead), init_probs = c(1, 0)) +
  event(name = death_event,
        options = c(yes, none),
        probs = c(pDie, leftover),
        transitions = c(Dead, stay)) +
  payoffs(names = c(utility))

pDie <- function(state, prob_die) prob_die * (state == "Alive")
utility <- function(state) 1 * (state == "Alive")

validate_twig(mytwig, params = list(prob_die = 0.1))
}
\seealso{
\code{\link[=run_twig]{run_twig()}}
}
